{
  "name": "diary",
  "dependencies": {
    "dotenv": "^16.0.3",
    "html-to-text": "^9.0.4",
    "masto": "^4.11.1",
    "openai": "^3.2.1"
  },
  "devDependencies": {
    "@types/node": "^18.14.6",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "start": "npm run clean && npm run build && node dist/diary.js",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  }
}
